/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { HouseService } from "../house.service";
import { HouseCreateInput } from "./HouseCreateInput";
import { House } from "./House";
import { HouseFindManyArgs } from "./HouseFindManyArgs";
import { HouseWhereUniqueInput } from "./HouseWhereUniqueInput";
import { HouseUpdateInput } from "./HouseUpdateInput";
import { KeyFindManyArgs } from "../../key/base/KeyFindManyArgs";
import { Key } from "../../key/base/Key";
import { KeyWhereUniqueInput } from "../../key/base/KeyWhereUniqueInput";

export class HouseControllerBase {
  constructor(protected readonly service: HouseService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: House })
  async createHouse(@common.Body() data: HouseCreateInput): Promise<House> {
    return await this.service.createHouse({
      data: {
        ...data,

        realEstateAgent: data.realEstateAgent
          ? {
              connect: data.realEstateAgent,
            }
          : undefined,
      },
      select: {
        address: true,
        createdAt: true,
        id: true,
        listedAt: true,
        price: true,

        realEstateAgent: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [House] })
  @ApiNestedQuery(HouseFindManyArgs)
  async houses(@common.Req() request: Request): Promise<House[]> {
    const args = plainToClass(HouseFindManyArgs, request.query);
    return this.service.houses({
      ...args,
      select: {
        address: true,
        createdAt: true,
        id: true,
        listedAt: true,
        price: true,

        realEstateAgent: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: House })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async house(
    @common.Param() params: HouseWhereUniqueInput
  ): Promise<House | null> {
    const result = await this.service.house({
      where: params,
      select: {
        address: true,
        createdAt: true,
        id: true,
        listedAt: true,
        price: true,

        realEstateAgent: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: House })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateHouse(
    @common.Param() params: HouseWhereUniqueInput,
    @common.Body() data: HouseUpdateInput
  ): Promise<House | null> {
    try {
      return await this.service.updateHouse({
        where: params,
        data: {
          ...data,

          realEstateAgent: data.realEstateAgent
            ? {
                connect: data.realEstateAgent,
              }
            : undefined,
        },
        select: {
          address: true,
          createdAt: true,
          id: true,
          listedAt: true,
          price: true,

          realEstateAgent: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: House })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteHouse(
    @common.Param() params: HouseWhereUniqueInput
  ): Promise<House | null> {
    try {
      return await this.service.deleteHouse({
        where: params,
        select: {
          address: true,
          createdAt: true,
          id: true,
          listedAt: true,
          price: true,

          realEstateAgent: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/keys")
  @ApiNestedQuery(KeyFindManyArgs)
  async findKeys(
    @common.Req() request: Request,
    @common.Param() params: HouseWhereUniqueInput
  ): Promise<Key[]> {
    const query = plainToClass(KeyFindManyArgs, request.query);
    const results = await this.service.findKeys(params.id, {
      ...query,
      select: {
        client: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        house: {
          select: {
            id: true,
          },
        },

        id: true,
        isActive: true,
        issuedAt: true,
        label: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/keys")
  async connectKeys(
    @common.Param() params: HouseWhereUniqueInput,
    @common.Body() body: KeyWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      keys: {
        connect: body,
      },
    };
    await this.service.updateHouse({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/keys")
  async updateKeys(
    @common.Param() params: HouseWhereUniqueInput,
    @common.Body() body: KeyWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      keys: {
        set: body,
      },
    };
    await this.service.updateHouse({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/keys")
  async disconnectKeys(
    @common.Param() params: HouseWhereUniqueInput,
    @common.Body() body: KeyWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      keys: {
        disconnect: body,
      },
    };
    await this.service.updateHouse({
      where: params,
      data,
      select: { id: true },
    });
  }
}
